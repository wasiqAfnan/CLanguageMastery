#include <stdio.h>

void main(){
    char a[20];
    printf("Enter your name : ");
    // gets function is used to get an string input form user
    gets(a);
    // puts function is used to print an string to terminal
    puts(a);
}